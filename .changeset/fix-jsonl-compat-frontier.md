---
"@martian-engineering/lossless-claw": patch
---

Keep the OpenClaw 2026.7.1 JSONL compatibility reader from treating its injected agent SQLite runtime-store hint as a sessions directory, while preserving explicit and custom store targets. Degraded live assembly now reserves renderer headroom before the host mid-turn prompt guard.
